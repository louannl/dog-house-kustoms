import { Fragment } from 'react';
import NavBar from './navigation/NavBar';

const Layout = () => {
  return (
    <Fragment>
      <NavBar />
      <main></main>
    </Fragment>
  );
};

export default Layout;
